// Transform file, generated from =PSPEC_FILE= by gigascope_make.pl
// Generates schema file =FUNS_H_FILE=

Output("=FUNS_H_FILE=");

#include "tr_funs_h_inc"

Transform Schema : TyDecl -> IO;

Schema(Pstruct =HDR_TYPE=)
Begin
 Each:  ty f -> "int get_pads_^f (struct packet *p, ^Ty(^ty) *r);"
End;

Schema(Pstruct =REC_TYPE=)
Begin
 Each:  ty f -> "int get_pads_^f (struct packet *p, ^Ty(^ty) *r);"
End;
